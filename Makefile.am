ACLOCAL_AMFLAGS = -I m4
SUBDIRS = src
EXTRA_DIST = doc/config doc/qfarm.sh
dist_doc_DATA = $(EXTRA_DIST)

clean-local:
	rm -rf $(DESTDIR)$(exec_prefix)

uninstall-local:
	rm -rf $(DESTDIR)$(docdir)


