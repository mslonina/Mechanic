ACLOCAL_AMFLAGS = -I m4
SUBDIRS = src doc examples

datadir=$(datarootdir)/${PACKAGE}

#clean-local:
#	rm -rf $(DESTDIR)$(exec_prefix)

uninstall-local:
	rm -rf $(docdir) $(datadir) $(includedir)/${PACKAGE} $(libdir)/${PACKAGE}


